.git
.env
.DS_Store
airflow_settings.yaml
__pycache__/
astro
.venv
airflow-webserver.pid
webserver_config.py
airflow.cfg
airflow.db
.como_utilizar.md
.vscode/
# Pontos de montagem dos repos do workspace (docker-compose.override.yml em dev,
# git archive do deploy/build_prod.sh em prod). Nada é versionado aqui.
include/my_ingestion/
dbt/my_analytics/
logs/

# DAGs só de dev: copiam dados de prod para o analytics_dev e para o lake
# local (/media/...), que só existem nesta máquina. Fora do versionamento, o
# deploy nunca as leva para o atb.
dags/dag_raw_pull_prod.py
dags/dag_presentation_export_prod.py

# backups de edição em massa (sed -i.bak)
*.bak
